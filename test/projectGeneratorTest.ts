/// <reference path="../typings/tsd.d.ts" />
/// <reference path="../typings/node/node.d.ts" />

import Generator from '../src/project_generator';

describe('Generator', () => {
    var subject : Generator;

    beforeEach(function () {
        subject = new Generator('test_project');
    });

    describe('#make', () => {
        it('should make project', () => {



//            var result : number = subject.add(2, 3);
//            if (result !== 5) {
//                throw new Error('Expected 2 + 3 = 5 but was ' + result);
//            }
        });
    });
});
